{
  "scripts": {
    "build:onchain": "esbuild src/onchain.js --bundle --format=iife --global-name=PactOnchainBundle --minify --outfile=dist/onchain.bundle.js",
    "start": "node server.js",
    "test": "npm run build:onchain && node --test tests/*.test.js",
    "test:e2e": "npm run build:onchain && playwright test"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "express": "^5.2.1",
    "viem": "^2.54.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "esbuild": "^0.28.1"
  }
}
